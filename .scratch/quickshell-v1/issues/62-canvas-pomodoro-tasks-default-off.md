Status: done

Category: bug

## Parent

`.scratch/quickshell-v1/issues/32-canvas-center-widgets.md`

## What to build

**Pomodoro** and **focus tasks** **canvas widgets** ship in v1 (issues 57–58) but default to **off** in `createDefaultCanvasWidgets()` and the starter template. Fresh `/home` loads show clock/welcome/quote/now-playing only — users expect timer and task glance widgets on the canvas without digging into settings.

Enable `pomodoro` and `focusTasks` by default in `createDefaultCanvasWidgets()` so new libraries and re-seeds get them. Existing libraries keep their saved toggles.

## Acceptance criteria

- [x] Starter template and `createDefaultCanvasWidgets()` set `pomodoro: true` and `focusTasks: true`
- [x] Fresh `/home` seed shows pomodoro and focus-tasks widgets on canvas (when tasks exist for focus-tasks)
- [x] Snapshot import/export round-trips widget toggles unchanged
- [x] Users can still disable either widget per workspace in settings

## Blocked by

None

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** Manual review on `/home` after seed — no pomodoro timer or today's tasks on canvas. Right-rim tools work; widgets exist but toggles default false in `src/canvas-widgets/config.ts`.
