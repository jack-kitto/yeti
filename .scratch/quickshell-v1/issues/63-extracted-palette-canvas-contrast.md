Status: ready-for-agent

Category: bug

## Parent

`.scratch/quickshell-v1/issues/44-theme-palette-extraction.md`

## What to build

Palette extraction from workspace background images (`mapExtractedColorsToPalette`) produces tokens that read acceptably on the **shell** rim but often fail on the **canvas** — clock, welcome, quote, and widget copy sit directly on the background image with no surface card. Extracted `text` and `surface` colors can be too close to dominant image hues; the result feels muddy and not premium.

Improve extraction (and/or canvas widget styling) so extracted themes meet minimum contrast for canvas content while preserving shell glass readability. Consider: WCAG-ish contrast checks against a sampled background, separate canvas-surface token, subtle text shadow, or semi-opaque widget backing — pick the smallest fix that lands well on starter Work/Personal images.

## Acceptance criteria

- [ ] Starter Work and Personal workspace themes pass a documented contrast rule for canvas text on their default background URLs
- [ ] Extracted palettes feel intentional (not raw dominant-pixel mapping) on at least the shipped starter images
- [ ] Manual palette overrides in settings still win over extraction
- [ ] Tests cover the contrast/mapping logic in `palette-extraction.ts`

## Blocked by

None

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** Shell rim contrast is good; canvas widgets (clock, quote, etc.) are hard to read on image-backed workspaces after async extraction. Current mapper picks highest-area light color as background and darkest as text without verifying separation against the actual backdrop.
