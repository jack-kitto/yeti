Status: ready-for-human

Category: bug

## Parent

`.scratch/quickshell-v1/issues/12-motion-visual-polish.md`

## What to build

During **workspace transition**, the shell frame expands toward the viewport edges (`workspace-transition.ts` + `getShellLayout()` `expand` lerp) but **rim chrome stays visible**: left **edge handles**, right **internal tool** handles, and settings affordance render at `z-[15]` above the shell canvas (`z-[10]`). Users see icons/handles floating over the transitioning shell instead of a clean full-screen cover.

Hide or fade rim chrome while `workspaceTransition.running` (or when `expand` > 0), or paint chrome behind an opaque transition overlay. Transition should read as the shell sealing the canvas, not a frame sliding while handles remain on top.

## Acceptance criteria

- [ ] Switching workspace (Tab, Control+Tab, control center) does not show edge/tool handles over the expanding shell
- [ ] Handles restore immediately after transition completes
- [ ] Rapid double-switch still lands on correct workspace with no stuck chrome
- [ ] No regression to normal hover/pin handle behavior at rest

## Blocked by

None — coordinate with issue 65 if transition model changes

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** Reproduced by code review. `ShellCanvas` at z-10; `shell-edge-chrome` at z-15 in `shell-edge-layer.tsx`. `expand` only lerps frame insets in `layout.ts`; chrome layer does not participate.
