Status: ready-for-human

Category: enhancement

## Parent

`.scratch/quickshell-v1/issues/12-motion-visual-polish.md`

## What to build

Revise **workspace transition** motion: instead of the shell **retracting/expanding** while the canvas scrolls underneath (current `expand` + `canvasOffsetX` model), the shell should **close fully** (hide the background image change), swap workspace + theme underneath, **morph palette colors** into the new workspace tokens, then **open** again.

This hides the jarring background swap and gives a deliberate "breathing" beat between contexts. Related bug: issue 64 (visible handles during current transition).

## Acceptance criteria

- [ ] Workspace switch reads as close → swap → color morph → open (human sign-off on timing)
- [ ] Background image change is not visible mid-transition
- [ ] Palette tokens interpolate or cross-fade during the closed phase (document chosen approach)
- [ ] All switch paths share the same transition (command bar Tab, Control+Tab, control center)
- [ ] Issue 64 chrome visibility resolved as part of this or separately

## Blocked by

Issue 64 (handles visible today) — may ship together

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** Product feedback during manual QA. Current implementation: `startWorkspaceTransition` in `workspace-transition.ts` — 520ms, expand peaks at t=0.5, swap at progress ≥ 0.5, canvas horizontal offset ±22% panel width. Framer Motion unused; canvas-drawn shell only.
