Status: done

Category: bug

## Parent

`.scratch/quickshell-v1/issues/53-tasks-flyout-layout-polish.md`

## What to build

Tasks **internal tool** flyout regressed after QA layout pass: rows are cramped, task title overlaps action buttons, and the wider notch still feels unusable at real content sizes.

Redesign task row layout so title, estimate field, and actions never collide — e.g. dedicated title row, estimate on its own line or column, action grid with consistent min widths. Verify at narrow right-rim notch width and with long task titles.

## Acceptance criteria

- [ ] No text/button overlap at default tasks flyout size (`shell-edge-layer` default menu size for `__tool_tasks__`)
- [ ] Long task titles wrap or truncate without covering controls
- [ ] Estimate input and action buttons remain tappable
- [ ] `tasks-flyout-layout.test.ts` assertions updated if layout contract changes
- [ ] Human sign-off on flyout density

## Blocked by

None

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** User report after session layout changes to `tasks-flyout.tsx` / `globals.css`. Issue 53 marked done; this is a regression follow-up.
