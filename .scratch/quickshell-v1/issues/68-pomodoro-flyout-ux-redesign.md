Status: done

Category: bug

## Parent

`.scratch/quickshell-v1/issues/54-pomodoro-flyout-clarity.md`

## What to build

Pomodoro **internal tool** flyout still confuses users after issue 54:

- Idle/running display reads as **break time** rather than clearly showing **work interval** vs break interval for the active **focus split**
- After **Start focus** on a task, user cannot easily **choose or change split** before pressing Start — split picker is buried below scroll
- Verify custom split inputs accept values **> 60 minutes** (issue 56); if browser/UI still blocks, fix

Redesign flyout information hierarchy: split picker + upcoming work/break durations visible when a task is armed; show full split summary (`work / short / long`) and highlight **current phase** duration.

## Acceptance criteria

- [ ] Idle state clearly shows work-phase duration for selected split (not break-only)
- [ ] When `activeTaskId` set, split picker and Start are reachable without scrolling past custom form on typical viewport
- [ ] Custom split work/short/long accept positive integers with no artificial 60-minute cap
- [ ] Running state shows phase label + correct remaining time for current phase
- [ ] Human sign-off on pomodoro flyout clarity

## Blocked by

None — pairs with issue 67

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** User report. Issue 54/55/56/61 marked done; manual QA suggests spec not met in practice or regression from later edits.
