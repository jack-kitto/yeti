Status: done

Category: enhancement

## Parent

`.scratch/quickshell-v1/issues/57-canvas-pomodoro-widget.md`

## What to build

**Canvas pomodoro** widget should include a premium **particle hourglass** (or similar) ambient visualization **linked to timer progress** — container of particles/sand that empties as `endsAt` approaches, synced with pomodoro or **focus countdown** mode.

When timer runs, hourglass is the hero visual; large **MM:SS** replaces wall clock (already shipped in session). When idle, hourglass optional/at rest or hidden per design.

Reference: prior prototype removed in QA session; reimplement with canvas/WebGL or performant DOM particles. Low contrast, ambient — not competing with clock text.

## Acceptance criteria

- [ ] Animation progress derives from `displayPomodoroSeconds` / `endsAt` (no duplicate timer state)
- [ ] Runs smoothly during pomodoro and focus countdown modes
- [ ] Visual quality human sign-off (issue 12 bar)
- [ ] Pauses visually when timer paused
- [ ] Respects reduced-motion preference

## Blocked by

Issue 68 (flyout/timer clarity) optional

## Comments

> *This was generated by AI during triage.*

Issue 57 slice 2 marked done with CSS hourglass; removed in QA pass in favor of clock-only widget. User wants particle hourglass back, linked to timer.
