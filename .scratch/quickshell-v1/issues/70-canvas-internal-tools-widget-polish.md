Status: done

Category: enhancement

## Parent

`.scratch/quickshell-v1/issues/32-canvas-center-widgets.md`

## What to build

Elevate **focus tasks** and **pomodoro/timer** **canvas widgets** to the same visual standard as clock/quote/now-playing — deliberate layout, motion, and typography on the home canvas.

**Focus tasks widget (current):** top-right minimal list with hover scale + click to toggle done. Polish: refined enter/hover motion, readable contrast (pairs with issue 63), optional subtle stagger.

**Timer widget:** coordinates with issue 69 hourglass; phase label + task name hierarchy.

Reconcile `CONTEXT.md` **Canvas widget** copy — session removed canvas task/pomo **controls** (rim-only); domain doc still mentions primary actions on canvas. Update CONTEXT if glance-only is the decision.

## Acceptance criteria

- [ ] Tasks + timer widgets feel cohesive with canvas stack (issue 32 optical center + top-right tasks corner)
- [ ] Widgets enabled by default (issue 62) and visually intentional on starter template
- [ ] `CONTEXT.md` matches shipped interaction model (glance vs controls)
- [ ] Human sign-off on canvas internal-tool widgets

## Blocked by

Issue 63 (palette contrast) recommended first

## Comments

> *This was generated by AI during triage.*

**QA 2026-06-10:** User wants "equally impressive styles, animations" for home-screen task and pomo widgets.
